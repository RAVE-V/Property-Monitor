---
phase: 01-foundation-openrent-sourcing
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified: [
  "src/libs/shared/roi.ts",
  "src/apps/web/components/SidePanel.tsx",
  "src/apps/web/hooks/useSelection.ts",
  "src/apps/web/page.tsx"
]
autonomous: true
requirements: [CALC-01, MAP-04]
must_haves:
  truths:
    - "Profit calculator accurately subtracts expenses from income to show ROI"
    - "Side panel opens when a property marker is clicked on the map"
    - "Side panel allows user to override default profit inputs (e.g. rent, bills)"
  artifacts:
    - path: "src/libs/shared/roi.ts"
      provides: "Math for R2R-SA profit analysis"
    - path: "src/apps/web/components/SidePanel.tsx"
      provides: "UI for property details and ROI adjustments"
  key_links:
    - from: "src/apps/web/components/SidePanel.tsx"
      to: "src/libs/shared/roi.ts"
      via: "Reactive profit calculation"
---

<objective>
Implement the interactive property side-panel and the automated profit calculator.

Purpose: To allow users to analyze specific property deals and calculate their potential investment return.
Output: A side panel with an interactive ROI calculator.
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-openrent-sourcing/01-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ROI Calculation Logic</name>
  <files>src/libs/shared/roi.ts</files>
  <behavior>
    - Function `calculateROI(input: ROIInput): ROIResult`:
      - Monthly Profit = Monthly Income - (Rent + Bills + Management Fees).
      - ROI % = (Annual Profit / Setup Costs) * 100.
      - Handle edge cases like zero setup costs.
    - Export `ROIInput` and `ROIResult` interfaces.
  </behavior>
  <action>
    Implement the ROI calculation logic in a shared utility file.
    Include default assumptions for bills (e.g. 15% of rent) and fees (e.g. 10% of revenue).
  </action>
  <verify>
    <automated>node --test src/libs/shared/roi.test.ts</automated>
  </verify>
  <done>ROI math logic is implemented and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Side-Panel UI Component</name>
  <files>src/apps/web/components/SidePanel.tsx</files>
  <action>
    Create a `SidePanel` component using Tailwind CSS.
    - Sections: Property Header (Title, Price, Bed), Photo Carousel (placeholder), and ROI Calculator.
    - ROI Calculator: Input fields for Rent, Monthly Income (RevPAR), Bills, and Fees.
    - Results: Display Monthly Profit and ROI percentage prominently.
    - Use React state for input overrides.
  </action>
  <verify>
    <automated>npx tsc src/apps/web/components/SidePanel.tsx --noEmit</automated>
  </verify>
  <done>Side panel UI renders property details and profit calculations.</done>
</task>

<task type="auto">
  <name>Task 3: Property Selection Integration</name>
  <files>src/apps/web/page.tsx, src/apps/web/hooks/useSelection.ts, src/apps/web/components/Map.tsx</files>
  <action>
    Implement property selection state management.
    - Create `useSelection` hook to track the `selectedPropertyId`.
    - Update `Map.tsx` to add a click listener to the property layer:
      - Set `selectedPropertyId` on click.
      - Highlight the selected marker.
    - Update `page.tsx` to render the `SidePanel` when a property is selected.
  </action>
  <verify>
    <automated>npm run build --prefix src/apps/web</automated>
  </verify>
  <done>Clicking a marker opens the side panel with property-specific data.</done>
</task>

</tasks>

<success_criteria>
1. Clicking a property marker opens the side panel.
2. The side panel displays the correct property price and details.
3. Changing the 'Monthly Income' input instantly updates the 'Profit' and 'ROI' results.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-openrent-sourcing/01-04-SUMMARY.md`
</output>
