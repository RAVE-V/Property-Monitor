---
phase: 01-foundation-openrent-sourcing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "package.json",
  "docker-compose.yml",
  "drizzle.config.ts",
  "src/libs/database/schema.ts",
  "src/libs/database/db.ts",
  "src/libs/shared/types.ts"
]
autonomous: true
requirements: [DATA-01, CALC-01]
must_haves:
  truths:
    - "Database containers (PostGIS, Redis) are running and accessible"
    - "Core database schema for properties and jobs is defined and migrated"
    - "Shared types for properties and calculation inputs are available"
  artifacts:
    - path: "docker-compose.yml"
      provides: "PostGIS 17 and Redis 7 services"
    - path: "src/libs/database/schema.ts"
      provides: "Property and Job table definitions with spatial support"
    - path: "src/libs/shared/types.ts"
      provides: "TypeScript interfaces for properties and ROI math"
  key_links:
    - from: "src/libs/database/db.ts"
      to: "PostgreSQL"
      via: "Drizzle ORM connection"
---

<objective>
Setup the core infrastructure and data foundation for Property Intel.

Purpose: To establish the database schema, spatial capabilities, and shared types required for all subsequent sourcing and analysis features.
Output: A running PostGIS/Redis environment and a typed database schema.
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project Initialization & Infrastructure</name>
  <files>package.json, docker-compose.yml, .env.example</files>
  <action>
    Initialize the project with Fastify v5, Drizzle ORM, and TypeScript.
    Create a `docker-compose.yml` with:
    - PostGIS 17 (postgis/postgis:17-3.5)
    - Redis 7
    Setup `.env.example` with standard database and redis URLs.
    Use `npm init -y` and install core dependencies: `fastify`, `zod`, `drizzle-orm`, `pg`, `bullmq`, `typescript`.
  </action>
  <verify>
    <automated>docker compose up -d && docker ps | grep -E "postgis|redis"</automated>
  </verify>
  <done>Infrastructure containers running, package.json initialized with core stack.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Database Schema & Shared Types</name>
  <files>src/libs/database/schema.ts, src/libs/shared/types.ts</files>
  <behavior>
    - Define `Property` table with: id, portal_id (unique), url, title, price, bedrooms, property_type, location (geometry point), raw_data (jsonb), scraped_at.
    - Define `ScrapingJob` table for BullMQ state tracking.
    - Export TypeScript interfaces for Property that match the schema.
  </behavior>
  <action>
    Implement the Drizzle schema in `src/libs/database/schema.ts` using `drizzle-orm/pg-core`.
    Use `st_point` for spatial data.
    Create `src/libs/shared/types.ts` for domain types (Property, ROIInput).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Schema and types defined without compilation errors.</done>
</task>

<task type="auto">
  <name>Task 3: Drizzle Setup & Migration</name>
  <files>drizzle.config.ts, src/libs/database/db.ts</files>
  <action>
    Configure `drizzle.config.ts` for migrations.
    Create `src/libs/database/db.ts` to export the initialized Drizzle instance using `pg`.
    Generate and run the initial migration to create tables in PostGIS.
  </action>
  <verify>
    <automated>npx drizzle-kit generate && npx drizzle-kit migrate</automated>
  </verify>
  <done>Database migrated successfully, tables exist in PostgreSQL.</done>
</task>

</tasks>

<success_criteria>
1. `docker compose up` starts PostGIS and Redis.
2. `npx drizzle-kit generate` produces migration files.
3. Database contains `properties` and `scraping_jobs` tables.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-openrent-sourcing/01-01-SUMMARY.md`
</output>
