---
phase: 01-foundation-openrent-sourcing
plan: 05
type: execute
wave: 4
depends_on: ["01-02", "01-03"]
files_modified: [
  "src/apps/api/routes/properties.ts",
  "src/apps/web/components/Filters.tsx",
  "src/apps/web/page.tsx",
  "src/libs/scrapers/openrent.ts"
]
autonomous: true
requirements: [MAP-03, PLAN-02]
must_haves:
  truths:
    - "API filters properties based on price, bedrooms, and property type"
    - "Map refreshes markers instantly when a filter is changed"
    - "Property analysis shows basic planning indicators (e.g. HMO status)"
  artifacts:
    - path: "src/apps/web/components/Filters.tsx"
      provides: "Interactive UI for price and bed filters"
    - path: "src/apps/api/routes/properties.ts"
      provides: "Filter-aware property search"
  key_links:
    - from: "src/apps/web/components/Filters.tsx"
      to: "src/apps/api/routes/properties.ts"
      via: "Query parameters"
---

<objective>
Implement map filtering and basic planning indicators.

Purpose: To allow users to narrow down property searches and see initial regulatory signals.
Output: A functional filter bar and property planning indicators.
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-openrent-sourcing/01-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Filtered API Endpoints</name>
  <files>src/apps/api/routes/properties.ts</files>
  <behavior>
    - GET /properties:
      - Accepts `minPrice`, `maxPrice`, `minBedrooms`, `propertyType`.
      - Adds `where` clauses to the PostGIS query.
      - Combine filters with the existing `bbox` filter.
  </behavior>
  <action>
    Update the `properties` route in Fastify to parse and apply filter parameters using Drizzle `and()` and `eq()` / `gte()` / `lte()`.
    Ensure Zod schemas are updated for the new query parameters.
  </action>
  <verify>
    <automated>curl -f "http://localhost:3000/properties?bbox=-0.1,51.5,0.1,51.6&minPrice=1000&maxPrice=2000"</automated>
  </verify>
  <done>API returns filtered property GeoJSON.</done>
</task>

<task type="auto">
  <name>Task 2: Filter Bar Component</name>
  <files>src/apps/web/components/Filters.tsx, src/apps/web/page.tsx</files>
  <action>
    Create a `Filters` component:
    - Dropdowns for Price range (min/max).
    - Select for Bedrooms (Studio to 5+).
    - Select for Property Type (Flat, House, Other).
    - "Clear Filters" button.
    In `page.tsx`, lift filter state and pass to `useProperties` hook.
  </action>
  <verify>
    <automated>npx tsc src/apps/web/components/Filters.tsx --noEmit</automated>
  </verify>
  <done>UI provides interactive filters that update the map.</done>
</task>

<task type="auto">
  <name>Task 3: Basic Planning Indicators</name>
  <files>src/libs/scrapers/openrent.ts, src/apps/web/components/SidePanel.tsx</files>
  <action>
    Update `scrapeOpenRent`:
    - Search description for keywords: "HMO", "Licensed", "Renovated", "Planning Permission".
    - Populate a `planningIndicators` array in the `Property` object.
    Update `SidePanel.tsx`:
    - Display indicators as tags/badges (e.g., "Potential HMO", "Recent Reno").
  </action>
  <verify>
    <automated>npx ts-node src/libs/scrapers/openrent.ts --test-url="https://www.openrent.co.uk/property-to-rent/london/hmo-licensed-flat/56789"</automated>
  </verify>
  <done>Properties show basic planning signals in the analysis panel.</done>
</task>

</tasks>

<success_criteria>
1. Changing the price filter instantly reduces the number of markers on the map.
2. The property side panel shows an "HMO" indicator if the listing mentions it.
3. Filters persist when the map is moved (bbox updates, but filters remain).
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-openrent-sourcing/01-05-SUMMARY.md`
</output>
