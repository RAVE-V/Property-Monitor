---
phase: 01-foundation-openrent-sourcing
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [
  "src/apps/web/page.tsx",
  "src/apps/web/components/Map.tsx",
  "src/apps/web/hooks/useProperties.ts"
]
autonomous: true
requirements: [MAP-01, MAP-02, USER-01]
must_haves:
  truths:
    - "Map renders with MapLibre v5 using WebGPU"
    - "Property markers are clustered when zoomed out"
    - "Map updates markers based on current viewport bounds"
  artifacts:
    - path: "src/apps/web/components/Map.tsx"
      provides: "MapLibre GL instance with clustering"
    - path: "src/apps/web/hooks/useProperties.ts"
      provides: "SWR/React Query hook for fetching GeoJSON properties"
  key_links:
    - from: "src/apps/web/components/Map.tsx"
      to: "MapLibre GL JS"
      via: "WebGPU rendering engine"
---

<objective>
Setup the interactive map visualization with clustering.

Purpose: To provide the core visual interface for property intelligence.
Output: A React component rendering an interactive MapLibre map with property markers.
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/01-foundation-openrent-sourcing/01-01-PLAN.md
</context>

<interfaces>
From src/libs/shared/types.ts:
```typescript
export interface Property {
  id: string;
  portalId: string;
  url: string;
  title: string;
  price: number;
  bedrooms: number | null;
  propertyType: string | null;
  location: { lng: number, lat: number };
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Next.js & MapLibre Setup</name>
  <files>src/apps/web/page.tsx, src/apps/web/components/Map.tsx</files>
  <action>
    Initialize Next.js frontend if not already present.
    Install `maplibre-gl`.
    Create a `Map` component that initializes a MapLibre instance.
    - Set `map.config.renderEngine = 'webgpu'` if available (MapLibre v5).
    - Use a standard light vector tile style.
  </action>
  <verify>
    <automated>npm run dev --prefix src/apps/web (Manual check required for GPU rendering)</automated>
  </verify>
  <done>Next.js page renders a full-screen interactive map.</done>
</task>

<task type="auto">
  <name>Task 2: Property Data Hook</name>
  <files>src/apps/web/hooks/useProperties.ts</files>
  <action>
    Create a `useProperties` hook using `SWR` or `React Query`.
    - It should accept `bbox` (bounds) as a parameter.
    - Fetches GeoJSON from `/api/properties?bbox=...`.
    - Returns data, loading state, and error.
  </action>
  <verify>
    <automated>npx tsc src/apps/web/hooks/useProperties.ts --noEmit</automated>
  </verify>
  <done>Hook exists to fetch property data based on map bounds.</done>
</task>

<task type="auto">
  <name>Task 3: Marker Clustering</name>
  <files>src/apps/web/components/Map.tsx</files>
  <action>
    Implement marker clustering in the `Map` component.
    - Add a GeoJSON source for properties fetched from `useProperties`.
    - Configure `cluster: true` and `clusterMaxZoom: 14`.
    - Define layers for:
      - Clusters (circles with count text).
      - Individual markers (icons or small circles).
    Implement `moveend` event to update the `bbox` passed to the hook.
  </action>
  <verify>
    <automated>npm run build --prefix src/apps/web</automated>
  </verify>
  <done>Map clusters markers and refreshes data on movement.</done>
</task>

</tasks>

<success_criteria>
1. Map displays property markers from the API.
2. Zooming out clusters markers; zooming in expands them.
3. Panning the map triggers a new data fetch.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-openrent-sourcing/01-03-SUMMARY.md`
</output>
