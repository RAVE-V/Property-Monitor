---
phase: 02-intelligence-compliance-hotspots
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified: [
  "src/app/components/Map.tsx",
  "src/app/components/SidePanel.tsx",
  "src/app/hooks/useHotspots.ts",
  "src/app/hooks/useZones.ts"
]
autonomous: true
requirements: [HOT-01, PLAN-01]
must_haves:
  truths:
    - "Map renders a smooth heatmap for SA demand"
    - "Map renders transparent polygons for Article 4 zones"
    - "Side panel shows an Article 4 alert for properties in restricted zones"
  artifacts:
    - path: "src/app/components/Map.tsx"
      provides: "Heatmap and Fill layers for intelligence overlays"
    - path: "src/app/hooks/useHotspots.ts"
      provides: "Reactive demand data fetching"
  key_links:
    - from: "src/app/components/Map.tsx"
      to: "MapLibre"
      via: "Native heatmap rendering"
---

<objective>
Implement the hotspots and compliance visualizations on the interactive map.

Purpose: To allow users to visually identify high-profit areas and regulatory risks.
Output: An intelligent map interface with heatmap and boundary overlays.
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-intelligence-compliance-hotspots/02-CONTEXT.md
@.planning/phases/02-intelligence-compliance-hotspots/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Heatmap Layer Implementation</name>
  <files>src/app/components/Map.tsx, src/app/hooks/useHotspots.ts</files>
  <action>
    Implement `useHotspots` hook for viewport-based demand data.
    Update `Map.tsx` to add a `heatmap` layer:
    - Weight: `occupancy` property.
    - Colors: Blue -> Yellow -> Red gradient.
    - Opacity: Driven by the manual slider (Plan 04).
  </action>
  <verify>
    <automated>npx tsc src/app/components/Map.tsx --noEmit</automated>
  </verify>
  <done>Map successfully renders a demand heatmap overlay.</done>
</task>

<task type="auto">
  <name>Task 2: Article 4 Polygon Overlays</name>
  <files>src/app/components/Map.tsx, src/app/hooks/useZones.ts</files>
  <action>
    Implement `useZones` hook for fetching Article 4 boundaries.
    Update `Map.tsx` to add a `fill` layer for zones:
    - Style: Semi-transparent red/orange fill for restricted areas.
    - Interaction: Show zone name on hover.
  </action>
  <verify>
    <automated>npx tsc src/app/components/Map.tsx --noEmit</automated>
  </verify>
  <done>Map shows planning restricted areas as shaded polygons.</done>
</task>

<task type="auto">
  <name>Task 3: Side-Panel Compliance Alerts</name>
  <files>src/app/components/SidePanel.tsx</files>
  <action>
    Update `SidePanel.tsx`:
    - Display a clear "Article 4 Warning" badge if `property.isArticle4` is true.
    - Include a brief tooltip explaining the restriction.
  </action>
  <verify>
    <automated>npx tsc src/app/components/SidePanel.tsx --noEmit</automated>
  </verify>
  <done>Investors are immediately alerted to planning risks for selected deals.</done>
</task>

</tasks>

<success_criteria>
1. Hotspot heatmap is visible and correctly weighted by occupancy.
2. Article 4 zones appear as red polygons over the map.
3. Selected properties in Article 4 zones show a clear warning badge.
</success_criteria>

<output>
After completion, create `.planning/phases/02-intelligence-compliance-hotspots/02-03-SUMMARY.md`
</output>
