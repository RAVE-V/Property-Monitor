---
phase: 02-intelligence-compliance-hotspots
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified: [
  "src/app/components/Map.tsx",
  "src/app/components/DemandMenu.tsx",
  "src/app/hooks/useIsochrones.ts",
  "src/app/page.tsx"
]
autonomous: true
requirements: [HOT-02]
must_haves:
  truths:
    - "Map renders glowing pulses for demand hubs (Hospitals/Tourism)"
    - "User can toggle demand layers and adjust hotspot opacity"
    - "Travel-time radius (isochrone) is visible around selected hubs"
  artifacts:
    - path: "src/app/components/DemandMenu.tsx"
      provides: "UI controls for demand intelligence"
    - path: "src/app/hooks/useIsochrones.ts"
      provides: "Integration with OpenRouteService"
  key_links:
    - from: "src/app/hooks/useIsochrones.ts"
      to: "OpenRouteService"
      via: "Travel-time radius calculation"
---

<objective>
Implement demand cohort layers and travel-time filtering.

Purpose: To help investors find properties in high-demand "Supercommuter" and worker zones.
Output: A demand-aware map interface with travel-time analysis.
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-intelligence-compliance-hotspots/02-CONTEXT.md
@.planning/phases/02-intelligence-compliance-hotspots/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Demand Hub Visualization</name>
  <files>src/app/components/Map.tsx</files>
  <action>
    Update `Map.tsx` to add a `circle` layer for demand hubs:
    - Style: Glowing/pulsing circles (animated CSS or SVG).
    - Source: New `GET /api/hubs` endpoint.
  </action>
  <verify>
    <automated>npx tsc src/app/components/Map.tsx --noEmit</automated>
  </verify>
  <done>Major demand centers are visually highlighted on the map.</done>
</task>

<task type="auto">
  <name>Task 2: Travel-Time (Isochrone) Hook</name>
  <files>src/app/hooks/useIsochrones.ts</files>
  <action>
    Implement `useIsochrones` hook:
    - Calls OpenRouteService API with hub location and travel time (e.g. 10 mins).
    - Returns GeoJSON polygon of the reachable area.
  </action>
  <verify>
    <automated>npx tsc src/app/hooks/useIsochrones.ts --noEmit</automated>
  </verify>
  <done>System can generate travel-time radius polygons for any landmark.</done>
</task>

<task type="auto">
  <name>Task 3: Demand Intelligence UI</name>
  <files>src/app/components/DemandMenu.tsx, src/app/page.tsx</files>
  <action>
    Create a `DemandMenu` component:
    - Toggles for Hospitals, Landmarks, Infrastructure.
    - Slider for Hotspot Opacity.
    - Selection for Travel Time (5, 10, 15 mins).
    Integrate into `page.tsx`.
  </action>
  <verify>
    <automated>npx tsc src/app/components/DemandMenu.tsx --noEmit</automated>
  </verify>
  <done>UI provides controls for exploring and filtering by investment demand signals.</done>
</task>

</tasks>

<success_criteria>
1. User can toggle 'Hospitals' demand hubs on/off.
2. Hotspot opacity changes smoothly with the slider.
3. Clicking a hub shows a 10-minute travel-time polygon around it.
</success_criteria>

<output>
After completion, create `.planning/phases/02-intelligence-compliance-hotspots/02-04-SUMMARY.md`
</output>
