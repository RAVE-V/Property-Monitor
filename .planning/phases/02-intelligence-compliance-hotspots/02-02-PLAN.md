---
phase: 02-intelligence-compliance-hotspots
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [
  "src/libs/scrapers/dluhc.ts",
  "src/libs/scrapers/demand.ts",
  "src/apps/api/routes/hotspots.ts",
  "src/apps/api/server.ts"
]
autonomous: true
requirements: [DATA-04, PLAN-01]
must_haves:
  truths:
    - "DLUHC scraper can fetch and store Article 4 GeoJSON boundaries"
    - "Hotspot API returns GeoJSON feature collection with occupancy weights"
  artifacts:
    - path: "src/libs/scrapers/dluhc.ts"
      provides: "Article 4 boundary ingestion logic"
    - path: "src/apps/api/routes/hotspots.ts"
      provides: "GET /hotspots endpoint with viewport filtering"
  key_links:
    - from: "src/libs/scrapers/dluhc.ts"
      to: "DLUHC API"
      via: "GeoJSON boundary fetching"
---

<objective>
Ingest Article 4 compliance data and prepare the hotspot demand API.

Purpose: To populate the database with regulatory zones and provide demand signals for map visualization.
Output: A populated compliance database and a functional hotspots API.
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-intelligence-compliance-hotspots/02-CONTEXT.md
@.planning/phases/02-intelligence-compliance-hotspots/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DLUHC Article 4 Ingestion</name>
  <files>src/libs/scrapers/dluhc.ts</files>
  <action>
    Implement a scraper for the DLUHC Planning Data Platform.
    - Fetch Article 4 GeoJSON for target councils.
    - Upsert boundaries into the `article4Zones` table using `ST_GeomFromGeoJSON`.
  </action>
  <verify>
    <automated>npx ts-node src/libs/scrapers/dluhc.ts --council="birmingham"</automated>
  </verify>
  <done>Article 4 boundaries are successfully stored in PostGIS.</done>
</task>

<task type="auto">
  <name>Task 2: SA Demand Data Aggregation</name>
  <files>src/libs/scrapers/demand.ts</files>
  <action>
    Implement a mock demand aggregator (for Phase 2).
    - Generate/Fetch occupancy data points (Lat/Lng + Occupancy %) for major UK cities.
    - Store in a `demandPoints` table (add to schema if needed).
  </action>
  <verify>
    <automated>npx tsc src/libs/scrapers/demand.ts --noEmit</automated>
  </verify>
  <done>Database contains demand signal points for hotspot generation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Hotspots API Endpoint</name>
  <files>src/apps/api/routes/hotspots.ts, src/apps/api/server.ts</files>
  <behavior>
    - GET /hotspots:
      - Accepts `bbox`.
      - Returns GeoJSON of demand points with `occupancy` property.
      - Uses `ST_MakeEnvelope` for viewport filtering.
  </behavior>
  <action>
    Create the hotspots route in Fastify.
    Register the route in `server.ts`.
  </action>
  <verify>
    <automated>curl -f "http://localhost:3000/hotspots?bbox=-1.9,52.4,-1.8,52.5"</automated>
  </verify>
  <done>API successfully serves demand data for the map frontend.</done>
</task>

</tasks>

<success_criteria>
1. `GET /hotspots` returns GeoJSON data with occupancy weights.
2. `article4Zones` table contains real boundary data from DLUHC.
</success_criteria>

<output>
After completion, create `.planning/phases/02-intelligence-compliance-hotspots/02-02-SUMMARY.md`
</output>
