---
phase: 02-intelligence-compliance-hotspots
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "src/libs/database/schema.ts",
  "src/libs/shared/roi.ts",
  "src/libs/database/db.ts"
]
autonomous: true
requirements: [CALC-02, CALC-03, PLAN-01]
must_haves:
  truths:
    - "Article 4 zones table exists in PostGIS with spatial support"
    - "ROI engine correctly applies TOMS VAT logic (VAT on margin)"
    - "ROI engine defaults to Post-April 2025 UK tax rules"
  artifacts:
    - path: "src/libs/database/schema.ts"
      provides: "Article 4 zone and planning status schemas"
    - path: "src/libs/shared/roi.ts"
      provides: "TOMS-compliant financial modeling"
  key_links:
    - from: "src/libs/shared/roi.ts"
      to: "Big.js"
      via: "Precision financial math"
---

<objective>
Setup the intelligence schemas and implement the advanced 2025-compliant ROI engine.

Purpose: To provide the foundation for compliance checking and accurate financial analysis for UK investors.
Output: An intelligence-ready database and a robust financial modeling engine.
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-intelligence-compliance-hotspots/02-CONTEXT.md
@.planning/phases/02-intelligence-compliance-hotspots/02-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Article 4 Schema Implementation</name>
  <files>src/libs/database/schema.ts</files>
  <behavior>
    - Define `article4Zones` table: id, name, council_id, zone_type (HMO/SA), boundary (geometry polygon).
    - Add `isArticle4` and `article4ZoneId` to the `properties` table.
  </behavior>
  <action>
    Update the Drizzle schema in `src/libs/database/schema.ts` with the new spatial tables.
    Use `st_polygon` for boundary data.
  </action>
  <verify>
    <automated>npx tsc src/libs/database/schema.ts --noEmit</automated>
  </verify>
  <done>Schema updated to handle Article 4 zones and property compliance status.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Advanced ROI Engine with TOMS</name>
  <files>src/libs/shared/roi.ts</files>
  <behavior>
    - Update `calculateROI` to include `useTOMS` and `taxYear2025` flags.
    - If `useTOMS`: VAT = (Revenue - Direct Costs) * (20 / 120).
    - Apply Post-April 2025 rules (no FHL capital allowances).
    - Implement `breakEvenPrice(occupancyRate: number)` logic.
  </behavior>
  <action>
    Implement the enhanced ROI logic using `Big.js` in `src/libs/shared/roi.ts`.
    Include exports for new calculation results.
  </action>
  <verify>
    <automated>node --test src/libs/shared/roi.test.ts</automated>
  </verify>
  <done>ROI engine supports TOMS VAT and 2025 tax rules with high precision.</done>
</task>

<task type="auto">
  <name>Task 3: Spatial Compliance Checker</name>
  <files>src/libs/database/db.ts</files>
  <action>
    Implement `checkPropertyCompliance(propertyId: string)`:
    - Uses PostGIS `ST_Intersects` to find if the property point is inside any `article4Zones`.
    - Updates the property record with `isArticle4 = true`.
  </action>
  <verify>
    <automated>npx tsc src/libs/database/db.ts --noEmit</automated>
  </verify>
  <done>Utility created to automatically tag properties based on planning zones.</done>
</task>

</tasks>

<success_criteria>
1. `calculateROI` returns correct VAT on margin for a sample deal.
2. `article4Zones` table is correctly defined in the schema.
3. `checkPropertyCompliance` successfully intersects points with polygons.
</success_criteria>

<output>
After completion, create `.planning/phases/02-intelligence-compliance-hotspots/02-01-SUMMARY.md`
</output>
